-- config.lua
-- Configuration Options

local coeConfig = {}

coeConfig.WOBBLE = 4 -- a small amount of variance for teleport
coeConfig.SHIFT = 10 -- offset / shift from target city location for Silo / Teleporter
coeConfig.TP_ENERGY_REQ = 5000000 -- power required by teleporter
coeConfig.PLAYER_FORCE = "player"
coeConfig.SURFACE_NAME = "Earth"
coeConfig.RANDOM_CITY  = "Random City"
coeConfig.ROCKET_SILO  = "rocket-silo"

return coeConfig
